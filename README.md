# Hackaton-Flightdelay — Back-end (Boilerplate)

Este repositorio contiene el proyecto `flight-ontime` (back-end) como boilerplate.

Descripción breve:
- Proyecto Java + Maven.
- Estructura básica y configuración inicial para el hackathon.

Commit inicial: "Add boilerplate back-end for Hackaton-Flightdelay"

Si el `git push` falla por autenticación, configura un token de acceso (PAT) o usa una clave SSH.

Para subir localmente (ejemplo):
```
cd "C:\GitRepo\Back-end"
git init
git checkout -b main
git add .
git commit -m "Add boilerplate back-end for Hackaton-Flightdelay"
git remote add origin https://github.com/H12-25-L-Equipo-36/Hackaton-Flightdelay.git
git push -u origin main
```
